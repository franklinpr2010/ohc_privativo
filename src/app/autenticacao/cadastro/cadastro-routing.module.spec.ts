import { CadastroRouting.Module } from './cadastro-routing.module';

describe('CadastroRouting.Module', () => {
  it('should create an instance', () => {
    expect(new CadastroRouting.Module()).toBeTruthy();
  });
});
